<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mju.dao.UserDao" >
    <insert id="addUser" useGeneratedKeys="true" keyProperty="id">
        insert into t_user (phone, email, password, salt, createTime) values (#{phone},#{email},#{password},#{salt},#{createTime})
    </insert>
    <insert id="addUserInfo">
        insert into t_user_info(userId, nick, avatar, sign, gender, birth, createTime) values (#{userId},#{nick},#{avatar},#{sign},#{gender},#{birth},#{createTime})
    </insert>


    <select id="getUserByPhone" resultType="com.mju.domain.User">
        select * from t_user where phone = #{phone}
    </select>
    <select id="getUserById" resultType="com.mju.domain.User">
        select * from t_user where id = #{id}
    </select>
    <select id="getUserInfoById" resultType="com.mju.domain.UserInfo">
        select * from t_user_info where userId = #{userId}
    </select>
</mapper>
